with "../sxml";

project Exec
is
   for Main use ("exec.adb");
   for Object_Dir use "../obj";

   package Builder is
      for Global_Configuration_Pragmas use "../spark.adc";
   end Builder;

end Exec;
