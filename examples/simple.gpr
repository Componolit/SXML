with "../sxml";

project Simple
is
   for Main use ("simple.adb");
   for Object_Dir use "../obj";

   package Builder is
      for Global_Configuration_Pragmas use "../spark.adc";
   end Builder;

end Simple;
