with "aunit";
with "../../SXML";

project Tests is
   for Source_Dirs use (".");
   for Main use ("tests.adb");
   for Exec_Dir use "../../obj";

   package Linker is
      for Default_Switches ("Ada") use ("-mcmodel=medium");
   end Linker;
end Tests;
